---
# Dashboard Certificate (Both external and internal domains)
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wazuh-dashboard-tls
  namespace: wazuh
spec:
  secretName: wazuh-dashboard-tls
  issuerRef:
    name: vault-issuer
    kind: Issuer
  commonName: <path:argocd/data/argocd-vault-plugin/wazuh#dashboard_url>
  dnsNames:
    - <path:argocd/data/argocd-vault-plugin/wazuh#dashboard_url>
    - <path:argocd/data/argocd-vault-plugin/wazuh#dashboard_internal_url>
  duration: 2160h # 90 days
  renewBefore: 720h # 30 days before expiration

---
# Wazuh API Certificate (Both external and internal domains)
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wazuh-api-tls
  namespace: wazuh
spec:
  secretName: wazuh-api-tls
  issuerRef:
    name: vault-issuer
    kind: Issuer
  commonName: <path:argocd/data/argocd-vault-plugin/wazuh#api_url>
  dnsNames:
    - <path:argocd/data/argocd-vault-plugin/wazuh#api_url>
    - <path:argocd/data/argocd-vault-plugin/wazuh#api_internal_url>
  duration: 2160h # 90 days
  renewBefore: 720h # 30 days before expiration
